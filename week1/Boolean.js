let isTrue = true;
let isFalse = false;

// Falsy 값들 (false로 변환되는 값들)
let falsyValues = [false, 0, -0, 0n, "", null, undefined, NaN];

// Truthy 값들 (true로 변환되는 값들)
let truthyValues = [true, 1, -1, "문자열", [], {}, function(){}];